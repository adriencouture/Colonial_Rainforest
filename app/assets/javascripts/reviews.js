// Place all the behaviors and hooks related to the matching controller here.
// All this logic will automatically be available in application.js.
// You can use CoffeeScript in this file: http://coffeescript.org/
//
// $(document).on('ready page:load', function() {
//   $('#new_review').on('submit', function(event) {
//     event.preventDefault();
//
//     var self = $(this);
//
//     $.ajax({
      // url: self.attr('action'), // Event occurs on the Form
      // type: self.attr('method'), // type: 'POST',
      // dataType: 'script', // dataType: 'html'
      // data: self.serialize() // comment= blah + blah + blah
      // success: function(data) {
      //   if ( data ) { // check to make sure something was sent back, not just empty...
      //     $('.reviews-list').append(data);
      //     $('#review_comment').val('');
      //   }
      // }
//     });
//   });
// });
